import { createContext, useEffect, useReducer } from 'react';

const initialState = {
  isLoading: false,
  userInfo: null,
  todos: null
}

const handlers = {
  ADD: (state, action) => {

  },
  UPDATE: (state, action) => {

  },
  DELETE: (state, action) => {

  }
}